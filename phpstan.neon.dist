includes:
    - phpstan-baseline.neon

parameters:
    level: max
    paths:
        - src
        - config
        - database

    ignoreErrors:
        - identifier: missingType.generics
          path: src/Models/*.php

